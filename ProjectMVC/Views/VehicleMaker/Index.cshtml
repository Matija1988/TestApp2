@model List<ProjectService.Model.VehicleMakeDTORead> 

@{
    ViewData["Title"] = "Index";
}

<h1>Vehicle makers</h1>
<div class="row">
    <p class="col">
        <a asp-controller="VehicleMaker" asp-action="Create" class="btn btn-primary">Create New</a>
    </p>
</div>

<table id="tblData" class="table table-bordered table-striped" style="width:100%">
    <thead>
        <tr>
            <th>
                Abrv
                @* <a asp-controller="VehicleMaker" asp-action="Index" asp-route-sortOrder=@ViewData["AbrvSortParam"] class="text-black">
                    Abrv
                </a> *@
            </th>
            <th>
                Name
            </th>
            <th>
                
            </th>
        </tr>
    </thead>
    <tbody>

@*         @foreach (var item in Model.Items)
        {
            <tr>
                <td>
                </td>

                <td>
                    @Html.DisplayFor(modelItem => item.Abrv)
                </td>

                <td>
                    @Html.DisplayFor(modelItem => item.Name)
                </td>
                <td>
                    <a class="btn btn-secondary" asp-action="Edit" asp-controller="VehicleMaker" asp-route-id="@item.Id">Edit</a>
                    <a class="btn btn-danger" asp-action="Delete" asp-controller="VehicleMaker" asp-route-id="@item.Id"
                       onclick="return confirm('Are you sure you waht to delete this entry?')">Delete</a>
                </td>
            </tr>
        }
 *@
    </tbody>

</table>

@section Scripts{
    <script src ="~/js/VehicleMaker.js"></script>
}